# frozen_string_literal: true

RSpec.describe Simple::Telemetry do
  it "has a version number" do
    expect(Simple::Telemetry::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(true).to eq(true)
  end
end
